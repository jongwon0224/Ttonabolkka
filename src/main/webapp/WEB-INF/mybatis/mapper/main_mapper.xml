<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="main_mapper">
	
	<!-- 	메인페이지에서 지역별로 카드 하나씩 생성 -->
	<select id="findPlaceByRegion" resultType="Card">
			SELECT p.id, p.name, pi.image_url AS image_url
			FROM PLACES p, PLACES_IMG pi
			WHERE P.id = pi.id AND pi.no = 1
	</select>
	
</mapper>